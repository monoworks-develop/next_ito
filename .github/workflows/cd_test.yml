name: CD-Test

on: [push]

jobs: 
  CD:
    name: CD
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - run: echo secret "${{secrets.AZUREAPPSERVICE_PUBLISHPROFILE_07086D2D6CE54D0F8AC67DFCFB849E77 }}" | sed 's/./& /g'

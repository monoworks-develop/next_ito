name: hello world

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: 事前確認
        run: ls -la
      - name: 事前確認
        uses: actions/checkout@v2
      - name: 事後確認
        run: ls -la
